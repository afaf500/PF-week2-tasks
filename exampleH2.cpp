#include <iostream>
using namespace std;
main()
{
    int sum=0, num=0;

    while(num>=0)
    {
        sum=sum+num;
    
        cout<<"Enter a number: ";
        cin>>num;
    }
        cout<<"sum"<<sum;
        
    }
    
